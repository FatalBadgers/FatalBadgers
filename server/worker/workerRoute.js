//this stores all of our express routes for workers

var express = require('express');
var controller = require('./workerController');

var router = express.Router();

router.get('/asdfasdf',);

module.exports = router;